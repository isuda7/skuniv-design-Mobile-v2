import React from 'react';
import './style.scss';

const LearningHeadLinkBadge = (props) => {
	return (
		<div className='learning_head_badge'>
			{props.children}
		</div>

	)
}
export default LearningHeadLinkBadge
